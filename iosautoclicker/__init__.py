"""Briefcase entry-point package.

The application code lives in the `src` package (imports are `from src.*`).
Briefcase requires a top-level package whose name matches the app name
(`iosautoclicker`) and runs it as `python -m iosautoclicker`. This thin wrapper
provides that without renaming `src` or touching the existing code/tests.
"""

__version__ = "1.0.0"
